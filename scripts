# To build launcher from source
    # Download source from github
    # cd /{root folder of source files}
    # mvn clean package(you will need JDK7 and Maven installed to run this build launcher script)
    # Launcher will be built in the target folder named "ProsperCraft.jar"
    
    
# Commandline to build modpack package.(You will need to create a "src" folder with the mods and configs in the folder)

java -cp ProsperCraft.jar com.skcraft.launcher.builder.PackageBuilder --version "1.0" --manifest-dest "dist/manifest.json" --objects-dest "dist/objects" --files "src" --config "ZigZagv1.json" --objects-url "objects" --version-file "version.json" --libs-url "http://play.prospercraft.com/minecraft/libraries"
java -cp ProsperCraft.jar com.skcraft.launcher.builder.PackageBuilder --version "1.0" --manifest-dest "dist/manifest.json" --objects-dest "dist/objects" --files "src" --config "Ponies_and_Pretty_Things_v1.json" --objects-url "objects" --version-file "version.json" --libs-url "http://play.prospercraft.com/minecraft/libraries"




##command to update server with new package files, deleting old ones.
##ZigZag V1

java -cp ProsperCraft.jar com.skcraft.launcher.builder.PackageBuilder --version "1.0" --manifest-dest "dist/manifest.json" --objects-dest "dist/objects" --files "src" --config "ZigZagv1.json" --objects-url "objects" --version-file "version.json" --libs-url "http://play.prospercraft.com/minecraft/libraries"
rsync -del -ave ssh dehtrim@home.bennett-m.com:/var/www/html/minecraft/packages/zigzag1/ zigzag1/ 



